version: 2

sources:
  - name: token_transfers
    description:

    database: "{{ env_var('BQ_PUBLIC_DB') }}"
    schema: "{{ env_var('BQ_PUBLIC_SCHEMA') }} "

    tables:
      - name: token_transfers